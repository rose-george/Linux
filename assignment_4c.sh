#21.Write a function which will find out all the files owned by the current user in the directory

#-------------------------------

#22. Write a function to explain scope of global & local variables

#explain()
#{
#	echo "Values of temp and value are" $temp and $value
#	local temp=$((temp+2))
#        local value=$((value/2))
#	echo "After manipulation, temp is $temp and value is $value"
#}
#temp=5
#value=7
#explain
#if [ $temp -gt $value ]
#then
#	echo "Temp is greater than value."
#else
#	echo "Value is greater than temp."
#fi

#----------------------------

#24. Use awk to print the entire line from a file. Print different columns of the file using sed.
